You are a risk control expert. Analyze the following process: "{input}"

Return ONLY a valid JSON object with exactly these fields:
{
  "process_description": "brief description",
  "risks": ["risk1", "risk2", "risk3"],
  "controls": ["control1", "control2", "control3"]
}
No extra text, no markdown, just the JSON.